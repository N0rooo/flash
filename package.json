{
  "name": "flash",
  "version": "0.3.2",
  "private": true,
  "description": "Flash, le chien qui rapporte tes photos dans l'ordre",
  "scripts": {
    "dev": "tauri dev",
    "build": "TAURI_SIGNING_PRIVATE_KEY=\"$(cat src-tauri/updater.local.key)\" TAURI_SIGNING_PRIVATE_KEY_PASSWORD=\"\" tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}